apiVersion: v1
kind: Service
metadata:
  name: app-desafio-devops
  namespace: {{ .Values.desafioDevops.namespace.name }}
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: app-desafio-devops
  ports:
  - name: app-desafio-devops
    port: {{ .Values.desafioDevops.port }}
    targetPort: {{ .Values.desafioDevops.port }}
  type: ClusterIP