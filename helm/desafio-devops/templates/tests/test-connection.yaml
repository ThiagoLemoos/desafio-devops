apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "desafioDevops.fullname" . }}-test-connection"
  labels:
    {{- include "desafioDevops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "desafioDevops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
